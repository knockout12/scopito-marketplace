logger:
  level: INFO
swarm:
  port:
    offset: 10
  datasources:
    data-sources:
      MarketplaceDS:
        driver-name: mysql
        user-name: root
        password: Password1
        connection-url: jdbc:mysql://localhost:3306/webdb?autoReconnect=true&useSSL=false&serverTimezone=UTC
