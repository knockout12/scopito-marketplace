package com.scopito.marketplace.domainmodel.dao;

import com.scopito.marketplace.domainmodel.model.ServicesBacklogEntity;
import org.jboss.logging.Logger;

import javax.enterprise.context.RequestScoped;
import java.util.List;
public class ServicesBacklog {
/*
@RequestScoped
extends AbstractDao<ServicesBacklogEntity>
    private final Logger logger = Logger.getLogger(getClass());

    ServicesBacklog() {
        super(ServicesBacklogEntity.class);
    }

    public List<ServicesBacklogEntity> listAll() {
        logger.info("Reading all from named query 'StatusEntity.findAll'");
        return listAll("ServiceBacklog.findAll");
    }


    public List<ServicesBacklogEntity> listRange(int pageCount, int pageSize) {
        logger.info("Reading range from named query 'DroneOperatorProfile.findAll'");
        return listAllRange("ServiceBacklog.findAll", pageCount, pageSize);
    }
    */
}